import React from 'react';

import { SectionTitle } from '@templates/home/styles';

const index = () => {
  return (
    <div>
      <SectionTitle>Mis cursos</SectionTitle>
    </div>
  );
};

export default index;
