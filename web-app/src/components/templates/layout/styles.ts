import styled from '@emotion/styled';

export const ContentDiv = styled.div`
  box-sizing: border-box;
  position: fixed;
  top: 0;
  left: 0;
  width: calc(100% - 57px);
  margin-left: 57px;
  height: 100vh;

  padding: 20px;
`;
